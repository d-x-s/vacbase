package ca.ubc.cs304.project.ui;

        import javafx.scene.Node;

public class HelpfulFunctions {
    public static Node setLayout(Node node, int x, int y) {
        node.setLayoutX(x);
        node.setLayoutY(y);
        return node;
    }
}
